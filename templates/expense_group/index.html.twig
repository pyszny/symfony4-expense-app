{% extends 'base.html.twig' %}

{% block title %}ExpenseGroup index{% endblock %}

{% block body %}
    <h1>ExpenseGroup index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for expense_group in expense_groups %}
            <tr>
                <td>{{ expense_group.id }}</td>
                <td>{{ expense_group.name }}</td>
                <td>
                    <a href="{{ path('expense_group_show', {'id': expense_group.id}) }}">show</a>
                    <a href="{{ path('expense_group_edit', {'id': expense_group.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('expense_group_new') }}">Create new</a>
{% endblock %}
