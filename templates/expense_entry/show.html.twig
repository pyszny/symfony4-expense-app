{% extends 'base.html.twig' %}

{% block title %}ExpenseEntry{% endblock %}

{% block body %}
    <h1>ExpenseEntry</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense_entry.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ expense_entry.value }}</td>
            </tr>
            <tr>
                <th>Expense</th>
                <td>{{ expense_entry.expense }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ expense_entry.expense.category }}</td>
            </tr>
            <tr>
                <th>Group</th>
                <td>{{ expense_entry.expense.expensegroup }}</td>
            </tr>
            <tr>
                <th>Expense_date</th>
                <td>{{ expense_entry.expenseDate ? expense_entry.expenseDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Expense_entry</th>
                <td>{{ expense_entry.expenseEntry ? expense_entry.expenseEntry|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('expense_entry_index') }}">back to list</a>

    <a href="{{ path('expense_entry_edit', {'id': expense_entry.id}) }}">edit</a>

    {{ include('expense_entry/_delete_form.html.twig') }}
{% endblock %}
