{% extends 'base.html.twig' %}

{% block title %}Edit ExpenseEntry{% endblock %}

{% block body %}
    <h1>Edit ExpenseEntry</h1>

    {{ include('expense_entry/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('expense_entry_index') }}">back to list</a>

    {{ include('expense_entry/_delete_form.html.twig') }}
{% endblock %}
